public class toUpperCase{
	public static void main (String args[])
	{Console c = new Console();
	  c.println ("Ejemplo de uso de toUpperCase");
                String str="Hola Mundo";
	 str=str.toUpperCase(); // str queda como "HOLA MUNDO"
                c.println(str);	    //Opcionalmente puede llevar como argumento una constante
                }		    // que referencia a cada pais, con sus reglas.
}
